const validateCommand = (commandArray) => {
    const validation = {
        success: false,
        msg: '$-1\r\n'
    }

    // do the command validation here
    return validation;
}

module.exports = {
    validateCommand
}